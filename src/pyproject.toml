[build-system]
requires = ["maturin>=1.4,<2"]
build-backend = "maturin"

[project]
name = "numba_celltree_rust"
version = "0.1.0"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: Implementation :: CPython",
]
